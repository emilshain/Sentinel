# Report data URL.
# Default (unset): /report.json, served by the Vite dev/preview server from
# ../pipeline/reports/golden_run.json (see vite.config.js). Override to point at
# a backend API or a static asset URL.
VITE_REPORT_PATH=/report.json
